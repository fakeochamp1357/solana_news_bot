services:
  - type: worker
    name: solana-news-bot
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python solana_telegram_bot.py
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
